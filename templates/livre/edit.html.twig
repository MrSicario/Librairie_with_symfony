{% extends 'base.html.twig' %}

{% block title %}Modifier le livre - {{ livre.titre }}{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg">
                <div class="card-header bg-primary text-white">
                    <div class="d-flex justify-content-between align-items-center">
                        <h2 class="h4 mb-0">
                            <i class="fas fa-edit me-2"></i>Modifier le livre
                        </h2>
                        <a href="{{ path('app_livre_index') }}" class="btn btn-light btn-sm">
                            <i class="fas fa-arrow-left me-1"></i>Retour
                        </a>
                    </div>
                </div>
                <div class="card-body p-4">
                    {{ include('livre/_form.html.twig', {'button_label': '💾 Mettre à jour'}) }}
                    
                    <div class="mt-4 pt-3 border-top">
                        <div class="d-flex gap-2">
                            {{ include('livre/_delete_form.html.twig') }}
                            <a href="{{ path('app_livre_show', {'id': livre.id}) }}" class="btn btn-info">
                                <i class="fas fa-eye me-1"></i>Voir les détails
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}