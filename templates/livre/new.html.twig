{% extends 'base.html.twig' %}

{% block title %}Ajouter un nouveau livre{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg">
                <div class="card-header bg-success text-white">
                    <div class="d-flex justify-content-between align-items-center">
                        <h2 class="h4 mb-0">
                            <i class="fas fa-plus-circle me-2"></i>Ajouter un nouveau livre
                        </h2>
                        <a href="{{ path('app_livre_index') }}" class="btn btn-light btn-sm">
                            <i class="fas fa-arrow-left me-1"></i>Retour
                        </a>
                    </div>
                </div>
                <div class="card-body p-4">
                    {{ include('livre/_form.html.twig', {'button_label': '➕ Ajouter le livre'}) }}
                </div>
            </div>
            
            <div class="card mt-4">
                <div class="card-body text-center">
                    <h5 class="card-title">💡 Pourquoi partager vos livres ?</h5>
                    <p class="card-text text-muted">
                        En partant vos livres scolaires, vous aidez d'autres élèves tout en réduisant 
                        votre impact écologique. Chaque livre partagé, c'est une économie pour une famille 
                        et un geste pour la planète 🌍
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}