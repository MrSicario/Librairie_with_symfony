{{ form_start(form) }}
    <div class="row">
        <div class="col-md-12 mb-3">
            {{ form_label(form.titre, 'Titre du livre', {'label_attr': {'class': 'form-label fw-bold'}}) }}
            {{ form_widget(form.titre, {'attr': {'class': 'form-control form-control-lg', 'placeholder': 'Ex: Mathématiques Terminale S'}}) }}
            <div class="form-text">Entrez le titre complet du livre scolaire</div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_label(form.classe, 'Niveau scolaire', {'label_attr': {'class': 'form-label fw-bold'}}) }}
            {{ form_widget(form.classe, {'attr': {'class': 'form-control', 'placeholder': 'Ex: Terminale S, 3ème'}}) }}
        </div>
        
        <div class="col-md-6 mb-3">
            {{ form_label(form.etat, 'État du livre', {'label_attr': {'class': 'form-label fw-bold'}}) }}
            {{ form_widget(form.etat, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="row">
        <div class="col-md-12 mb-4">
            {{ form_label(form.ville, 'Ville', {'label_attr': {'class': 'form-label fw-bold'}}) }}
            {{ form_widget(form.ville, {'attr': {'class': 'form-control', 'placeholder': 'Ex: Paris, Lyon, Marseille'}}) }}
            <div class="form-text">La ville où le livre est disponible</div>
        </div>
    </div>

    <div class="d-flex gap-2">
        <button type="submit" class="btn btn-primary btn-lg">
            <i class="fas fa-save me-2"></i>{{ button_label|default('💾 Enregistrer') }}
        </button>
        <a href="{{ path('app_livre_index') }}" class="btn btn-secondary btn-lg">
            <i class="fas fa-times me-2"></i>Annuler
        </a>
    </div>
{{ form_end(form) }}